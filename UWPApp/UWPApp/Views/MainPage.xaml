<Page
    x:Class="UWPApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Margin="0,100,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ProgressRing IsActive="{Binding IsProgress}"/>
        <Button Content="Test" Command="{Binding EnumCommand}" Margin="0,10,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Height="50"/>
        <Border BorderBrush="DimGray" BorderThickness="2" Margin="0,10,0,0">
            <ScrollViewer  Width="400" Height="300">
                <TextBlock Text="{Binding LogMsg}"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</Page>
